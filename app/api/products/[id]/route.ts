import { NextRequest, NextResponse } from "next/server"; import { prisma } from "@/lib/prisma"; import { slugify } from "@/lib/utils";
function auth(req:NextRequest){return req.headers.get("x-admin-password")===process.env.ADMIN_PASSWORD}
export async function PUT(req:NextRequest,{params}:{params:Promise<{id:string}>}){if(!auth(req))return NextResponse.json({error:"Unauthorized"},{status:401});try{const {id}=await params;const b=await req.json();const p=await prisma.product.update({where:{id:Number(id)},data:{name:b.name,slug:slugify(b.slug||b.name),category:b.category,brand:b.brand,price:Number(b.price),oldPrice:b.oldPrice?Number(b.oldPrice):null,stock:Number(b.stock||0),image:b.image,shortDesc:b.shortDesc,description:b.description,specs:b.specs,featured:Boolean(b.featured)}});return NextResponse.json(p)}catch(e:any){return NextResponse.json({error:e.message||"Unable to update"},{status:400})}}
export async function DELETE(req:NextRequest,{params}:{params:Promise<{id:string}>}){if(!auth(req))return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;await prisma.product.delete({where:{id:Number(id)}});return NextResponse.json({ok:true})}
